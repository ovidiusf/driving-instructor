
const initTilt = () => {
  $('.galerie-img').tilt({
    maxTilt: 3,
  });
  $('.despremine-img').tilt({
    maxTilt: 2,
  });
  $('.testimonial-img').tilt({
    maxTilt: 3,
  });
};

export default initTilt;
